/**
 * Shared width contracts for Settings-like master/detail surfaces rendered as
 * generated CSS resources. CSS custom properties cannot be used in @media or
 * @container conditions, so query thresholds live here as one reviewed SSOT.
 */
export const SETTINGS_COMPACT_MAX = "37.5rem" as const;
