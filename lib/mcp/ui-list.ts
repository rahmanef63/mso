import { MSO_LOGO_SVG } from "@/lib/presentation/logo";
import { widgetResourceMeta } from "./ui-config";
import { MSO_WIDGET_THEME_SCRIPT, MSO_WIDGET_TOKENS } from "./ui-widget-tokens";

export const MSO_LIST_URI = "ui://mso/list-v1.html";
const MIME = "text/html;profile=mcp-app";

const html = String.raw`<meta charset="utf-8"><main class="mso-list" aria-live="polite">
<style>
${MSO_WIDGET_TOKENS}
*{box-sizing:border-box}html,body{margin:0;min-width:0;background:transparent;color:var(--text)}body{padding:7px;font-family:var(--font-body)}.shell{overflow:hidden;border:1px solid var(--sep-strong);border-radius:var(--radius-panel);background:var(--surface);box-shadow:var(--shadow-pop)}.head{display:flex;align-items:flex-start;gap:10px;padding:11px 12px;border-bottom:1px solid var(--sep)}.mark{display:grid;width:30px;height:30px;flex:0 0 30px;place-items:center;border-radius:7px;background:var(--text);color:var(--surface)}.mark svg{display:block;width:100%;height:100%}.copy{min-width:0;flex:1}.eyebrow{font-family:var(--font-mono);font-size:8px;font-weight:650;letter-spacing:.11em;text-transform:uppercase;color:var(--text-dim)}h2{margin:2px 0 0;font-family:var(--font-display);font-size:13px;font-weight:720;letter-spacing:-.026em;line-height:1.3}.description{margin:4px 0 0;font-size:10px;line-height:1.45;color:var(--text-dim);overflow-wrap:anywhere}.count{flex:0 0 auto;border:1px solid var(--sep);border-radius:999px;padding:4px 7px;font-family:var(--font-mono);font-size:8px;color:var(--text-dim)}.filter{padding:8px 10px;border-bottom:1px solid var(--sep);background:var(--inset)}.filter input{width:100%;min-height:32px;border:1px solid var(--sep-strong);border-radius:8px;background:var(--field);color:var(--text);padding:6px 9px;font:inherit;font-size:10px;outline:none}.filter input:focus{border-color:var(--os-accent);box-shadow:0 0 0 2px color-mix(in srgb,var(--os-accent) 17%,transparent)}.items{display:grid}.items.grid{grid-template-columns:repeat(2,minmax(0,1fr));gap:8px;padding:8px}.items.grid .item{border:1px solid var(--sep);border-radius:9px}.item{min-width:0;display:grid;grid-template-columns:auto minmax(0,1fr) auto;gap:9px;align-items:center;padding:10px 12px;border-bottom:1px solid var(--sep)}.items:not(.grid) .item:last-child{border-bottom:0}.icon{display:grid;width:30px;height:30px;place-items:center;border:1px solid var(--sep-strong);border-radius:8px;background:var(--field);font-family:var(--font-mono);font-size:10px;font-weight:750;color:var(--text-dim)}.main{min-width:0}.title-row{display:flex;align-items:center;gap:6px;min-width:0}.title{min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font-size:10.5px;font-weight:700}.badge{flex:0 0 auto;border:1px solid var(--sep);border-radius:999px;padding:2px 5px;font-family:var(--font-mono);font-size:7.5px;color:var(--text-dim)}.badge.success{border-color:color-mix(in srgb,var(--success-text) 40%,var(--sep));color:var(--success-text)}.badge.warning{border-color:color-mix(in srgb,var(--warning) 45%,var(--sep));color:var(--warning)}.badge.error{border-color:color-mix(in srgb,var(--destructive-text) 40%,var(--sep));color:var(--destructive-text)}.badge.info{border-color:color-mix(in srgb,var(--info) 40%,var(--sep));color:var(--info)}.subtitle{margin-top:2px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font-size:9px;color:var(--text-dim)}.meta{display:flex;gap:5px;flex-wrap:wrap;margin-top:5px}.meta span{font-family:var(--font-mono);font-size:9px;color:var(--text-dim)}.actions{display:flex;gap:5px;align-items:center}.actions button,.footer button{appearance:none;min-height:28px;border:1px solid var(--sep-strong);border-radius:8px;background:transparent;color:var(--text);padding:5px 8px;font:inherit;font-size:9px;font-weight:680;cursor:pointer}.actions button:hover,.footer button:hover{background:var(--hover-strong)}.actions button.primary,.footer button.primary{border-color:var(--text);background:var(--text);color:var(--surface)}.empty{padding:18px 12px;text-align:center;font-size:10px;color:var(--text-dim)}.footer{display:flex;align-items:center;gap:6px;justify-content:flex-end;border-top:1px solid var(--sep);padding:8px 10px}.feedback{margin-right:auto;font-size:9px;color:var(--text-dim)}@media(max-width:560px){body{padding:4px}.items.grid{grid-template-columns:1fr}.item{grid-template-columns:auto minmax(0,1fr)}.actions{grid-column:2;justify-content:flex-start;flex-wrap:wrap}.head{padding:10px}.item{padding:9px 10px}}
</style>
<section class="shell" aria-label="MSO list">
<header class="head"><div class="mark" aria-hidden="true">${MSO_LOGO_SVG}</div><div class="copy"><div class="eyebrow">Collection</div><h2 id="title">MSO list</h2><p class="description" id="description" hidden></p></div><span class="count" id="count">0</span></header>
<div class="filter" id="filter-wrap" hidden><input id="filter" type="search" maxlength="120" autocomplete="off" placeholder="Filter items" aria-label="Filter items"></div>
<div class="items" id="items"></div><div class="empty" id="empty" hidden>No items.</div>
<footer class="footer" id="footer" hidden><span class="feedback" id="feedback" role="status" aria-live="polite"></span><div class="actions" id="footer-actions"></div></footer>
</section>
<script>(()=>{
${MSO_WIDGET_THEME_SCRIPT}
const $=id=>document.getElementById(id);const title=$("title"),description=$("description"),count=$("count"),items=$("items"),empty=$("empty"),filterWrap=$("filter-wrap"),filter=$("filter"),footer=$("footer"),footerActions=$("footer-actions"),feedback=$("feedback");let current=null;
const text=(v,f="")=>typeof v==="string"&&v.trim()?v.trim():f;const rows=(v,max)=>Array.isArray(v)?v.filter(x=>x&&typeof x==="object"&&!Array.isArray(x)).slice(0,max):[];const icons={project:"PRJ",file:"DOC",folder:"DIR",workflow:"WF",session:"SES",app:"APP",integration:"INT",agent:"AGT",server:"SRV",code:"CODE",item:"•"};
function normalize(raw){if(!raw||typeof raw!=="object")return null;return{title:text(raw.title,"MSO list"),description:text(raw.description),layout:raw.layout==="grid"?"grid":"list",searchable:raw.searchable===true,emptyMessage:text(raw.emptyMessage,"No items."),items:rows(raw.items,40).map((row,index)=>({id:text(row.id,"item-"+(index+1)),title:text(row.title,"Item "+(index+1)),subtitle:text(row.subtitle),badge:text(row.badge),status:["neutral","info","success","warning","error"].includes(row.status)?row.status:"neutral",icon:icons[row.icon]?row.icon:"item",meta:rows(row.meta,4).map(m=>({label:text(m.label),value:text(m.value)})),actions:rows(row.actions,2).map((a,i)=>({id:text(a.id,"action-"+(i+1)),label:text(a.label,"Open"),style:a.style==="primary"?"primary":"secondary",prompt:text(a.prompt)})).filter(a=>a.prompt)})),actions:rows(raw.actions,3).map((a,i)=>({id:text(a.id,"global-"+(i+1)),label:text(a.label,"Continue"),style:a.style==="primary"?"primary":"secondary",prompt:text(a.prompt)})).filter(a=>a.prompt)}}
function clear(node){while(node.firstChild)node.removeChild(node.firstChild)}function el(tag,cls,val){const n=document.createElement(tag);if(cls)n.className=cls;if(val!==undefined)n.textContent=String(val);return n}
async function follow(action,item){if(!action?.prompt)return;const context=item?"\n\nMSO list selection:\n"+JSON.stringify({id:item.id,title:item.title}).slice(0,1000):"";feedback.textContent="Sending to chat…";try{if(!window.openai||typeof window.openai.sendFollowUpMessage!=="function")throw new Error("unsupported");await window.openai.sendFollowUpMessage({prompt:action.prompt+context});feedback.textContent="Sent to chat."}catch(_){feedback.textContent="Continue in the chat."}}
function renderAction(action,item){const b=el("button",action.style||"secondary",action.label);b.type="button";b.addEventListener("click",()=>follow(action,item));return b}
function renderItems(query=""){clear(items);const q=query.trim().toLowerCase();const visible=current.items.filter(row=>!q||[row.title,row.subtitle,row.badge,...row.meta.flatMap(m=>[m.label,m.value])].join(" ").toLowerCase().includes(q));items.className="items"+(current.layout==="grid"?" grid":"");count.textContent=String(visible.length)+"/"+String(current.items.length);empty.hidden=visible.length>0;empty.textContent=q?"No matching items.":current.emptyMessage;for(const row of visible){const item=el("article","item");item.dataset.itemId=row.id;item.append(el("div","icon",icons[row.icon]||"•"));const main=el("div","main"),tr=el("div","title-row");tr.append(el("div","title",row.title));if(row.badge)tr.append(el("span","badge "+row.status,row.badge));main.append(tr);if(row.subtitle)main.append(el("div","subtitle",row.subtitle));if(row.meta.length){const meta=el("div","meta");for(const m of row.meta)meta.append(el("span","",m.label?(m.label+": "+m.value):m.value));main.append(meta)}item.append(main);if(row.actions.length){const actions=el("div","actions");for(const a of row.actions)actions.append(renderAction(a,row));item.append(actions)}items.append(item)}}
function persist(){try{if(window.openai&&typeof window.openai.setWidgetState==="function")window.openai.setWidgetState({modelContent:"MSO list: "+(current?.title||"collection"),privateContent:{query:filter.value},imageIds:[]})}catch(_){}}
function render(raw){const next=normalize(raw);if(!next)return;current=next;title.textContent=next.title;description.textContent=next.description;description.hidden=!next.description;filterWrap.hidden=!next.searchable;filter.value="";renderItems();clear(footerActions);footer.hidden=!next.actions.length;for(const a of next.actions)footerActions.append(renderAction(a));persist()}
filter.addEventListener("input",()=>{renderItems(filter.value);persist()});function readHostOutput(){const output=window.openai&&window.openai.toolOutput;if(output)render(output)}window.addEventListener("message",event=>{if(event.source!==window.parent)return;const message=event.data;if(message&&message.method==="ui/notifications/tool-result"&&message.params&&message.params.structuredContent)render(message.params.structuredContent)},{passive:true});window.addEventListener("openai:set_globals",()=>{applyHostTheme();readHostOutput()},{passive:true});applyHostTheme();readHostOutput();
})();</script></main>`;

export const MSO_LIST_RESOURCE = {
  uri: MSO_LIST_URI,
  name: "MSO List",
  description: "Compact searchable list or grid for model-selected MSO items.",
  mimeType: MIME,
  text: html,
  _meta: widgetResourceMeta(
    "Compact MSO collection view for model-selected projects, files, sessions, workflows, apps, integrations, agents, and other bounded items. Actions return user-approved follow-ups to the chat and never execute mutations directly.",
  ),
} as const;
