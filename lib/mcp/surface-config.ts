// Compatibility entry point; the reviewed registry is shared with the authenticated cockpit.
export { configuredSurfaceApps } from "@/lib/surfaces/config";
export type { SurfaceApp, SurfaceRenderer, SurfacePresentation, SurfaceEnvironment } from "@/lib/contracts/surface-app";
