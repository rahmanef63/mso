import { widgetResourceMeta } from "./ui-config";

export const MSO_BLOCK_URI = "ui://mso/block-v1.html";
const MIME = "text/html;profile=mcp-app";

const html = String.raw`<main class="mso-block" aria-live="polite">
<style>
:root{color-scheme:light dark;font-family:ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;--line:color-mix(in srgb,CanvasText 14%,transparent);--muted:color-mix(in srgb,CanvasText 58%,transparent);--soft:color-mix(in srgb,CanvasText 5%,Canvas);--accent:#7c3aed;--success:#16a34a;--warning:#d97706;--danger:#dc2626}*{box-sizing:border-box}html,body{margin:0;min-width:0;background:transparent;color:CanvasText}body{padding:7px}.card{overflow:hidden;border:1px solid var(--line);border-radius:16px;background:color-mix(in srgb,Canvas 97%,transparent);box-shadow:0 6px 24px color-mix(in srgb,#000 10%,transparent)}.head{display:flex;align-items:flex-start;gap:10px;padding:12px 12px 9px}.mark{display:grid;width:30px;height:30px;flex:0 0 30px;place-items:center;border-radius:9px;background:color-mix(in srgb,var(--accent) 17%,Canvas);font-size:12px;font-weight:850}.copy{min-width:0;flex:1}.eyebrow{font-size:8px;font-weight:750;letter-spacing:.09em;text-transform:uppercase;color:var(--muted)}h2{margin:2px 0 0;font-size:13px;line-height:1.3}.description{margin:4px 0 0;font-size:10px;line-height:1.45;color:var(--muted);overflow-wrap:anywhere}.status{flex:0 0 auto;border:1px solid var(--line);border-radius:999px;padding:4px 7px;font-size:8px;font-weight:800;letter-spacing:.06em;text-transform:uppercase}.status.pending{background:color-mix(in srgb,var(--accent) 12%,Canvas)}.status.success{background:color-mix(in srgb,var(--success) 12%,Canvas)}.status.warning{background:color-mix(in srgb,var(--warning) 13%,Canvas)}.status.error{background:color-mix(in srgb,var(--danger) 12%,Canvas)}.section{border-top:1px solid var(--line);padding:9px 12px}.section-title{margin:0 0 7px;font-size:8px;font-weight:750;letter-spacing:.08em;text-transform:uppercase;color:var(--muted)}.checks,.outputs{display:grid;gap:5px}.check,.output{display:grid;grid-template-columns:auto minmax(0,1fr) auto;align-items:center;gap:7px;min-height:27px}.dot{width:7px;height:7px;border-radius:50%;background:color-mix(in srgb,CanvasText 30%,transparent)}.dot.pass{background:var(--success)}.dot.warn{background:var(--warning)}.dot.fail{background:var(--danger)}.check strong,.output strong{min-width:0;font-size:10px}.detail,.value{min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font-size:9px;color:var(--muted)}.fields{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px}.field{min-width:0}.field.full{grid-column:1/-1}.field label{display:block;margin-bottom:4px;font-size:9px;font-weight:650}.required{color:var(--danger)}input,textarea{width:100%;min-height:34px;border:1px solid var(--line);border-radius:9px;background:var(--soft);color:CanvasText;padding:7px 8px;font:inherit;font-size:10px;outline:none}textarea{min-height:72px;resize:vertical}input:focus,textarea:focus{border-color:color-mix(in srgb,var(--accent) 55%,var(--line));box-shadow:0 0 0 2px color-mix(in srgb,var(--accent) 15%,transparent)}.boolean{display:flex;min-height:34px;align-items:center;gap:7px;border:1px solid var(--line);border-radius:9px;padding:7px 8px;background:var(--soft);font-size:10px}.boolean input{width:15px;min-height:15px;margin:0}.readonly{min-height:34px;border:1px solid var(--line);border-radius:9px;padding:8px;background:var(--soft);font-size:10px;overflow-wrap:anywhere}.actions{display:flex;align-items:center;justify-content:flex-end;gap:6px;flex-wrap:wrap}.feedback{min-width:0;flex:1;font-size:9px;color:var(--muted)}button{appearance:none;min-height:32px;border:1px solid var(--line);border-radius:9px;background:var(--soft);color:CanvasText;padding:6px 10px;font:inherit;font-size:10px;font-weight:700;cursor:pointer}button.primary{background:color-mix(in srgb,var(--accent) 17%,Canvas)}button.danger{background:color-mix(in srgb,var(--danger) 12%,Canvas)}button:disabled{cursor:default;opacity:.55}button:focus-visible{outline:2px solid Highlight;outline-offset:2px}.empty{padding:4px 0;font-size:10px;color:var(--muted)}@media(max-width:520px){body{padding:4px}.card{border-radius:13px}.head{padding:10px}.section{padding:8px 10px}.fields{grid-template-columns:1fr}.field.full{grid-column:auto}.actions{align-items:stretch}.feedback{width:100%;flex-basis:100%}button{flex:1}}
</style>
<section class="card" aria-label="MSO interaction block">
  <header class="head">
    <div class="mark" aria-hidden="true">M</div>
    <div class="copy"><div class="eyebrow" id="kind">Action</div><h2 id="title">MSO block</h2><p class="description" id="description" hidden></p></div>
    <span class="status neutral" id="status">Ready</span>
  </header>
  <section class="section" id="checks-section" hidden><h3 class="section-title">Validation</h3><div class="checks" id="checks"></div></section>
  <form id="form">
    <section class="section" id="fields-section" hidden><h3 class="section-title">Input</h3><div class="fields" id="fields"></div></section>
    <section class="section" id="outputs-section" hidden><h3 class="section-title">Output</h3><div class="outputs" id="outputs"></div></section>
    <footer class="section actions" id="actions-section" hidden><span class="feedback" id="feedback" role="status" aria-live="polite"></span><div class="actions" id="actions"></div></footer>
  </form>
</section>
<script>(()=>{
const $=id=>document.getElementById(id);const kindEl=$("kind"),titleEl=$("title"),descriptionEl=$("description"),statusEl=$("status"),form=$("form"),feedback=$("feedback");
const checksSection=$("checks-section"),checksEl=$("checks"),fieldsSection=$("fields-section"),fieldsEl=$("fields"),outputsSection=$("outputs-section"),outputsEl=$("outputs"),actionsSection=$("actions-section"),actionsEl=$("actions");
let current=null;let confirmId="";let confirmTimer=0;
const text=(value,fallback="")=>typeof value==="string"&&value.trim()?value.trim():fallback;
const scalar=value=>typeof value==="string"||typeof value==="number"||typeof value==="boolean"||value===null?value:"";
const clear=node=>{while(node.firstChild)node.removeChild(node.firstChild)};
const el=(tag,className,value)=>{const node=document.createElement(tag);if(className)node.className=className;if(value!==undefined)node.textContent=String(value);return node};
function workflowFallback(raw){if(!raw||typeof raw!=="object"||typeof raw.workflowId!=="string")return raw;return{kind:"validation",title:raw.active===false?"MSO workflow closed":"MSO workflow started",description:text(raw.intent,"Workflow orchestration continues in the background."),status:raw.active===false?"success":"pending",fields:[],checks:[],outputs:[...(raw.project?[{label:"Project",value:raw.project}]:[]),{label:"Steps",value:Number(raw.stepCount)||0}],actions:[]}}
function normalize(raw){raw=workflowFallback(raw);if(!raw||typeof raw!=="object")return null;return{kind:["validation","crud","action"].includes(raw.kind)?raw.kind:"action",title:text(raw.title,"MSO block"),description:text(raw.description,""),status:["neutral","pending","success","warning","error"].includes(raw.status)?raw.status:"neutral",fields:Array.isArray(raw.fields)?raw.fields.slice(0,20):[],checks:Array.isArray(raw.checks)?raw.checks.slice(0,20):[],outputs:Array.isArray(raw.outputs)?raw.outputs.slice(0,20):[],actions:Array.isArray(raw.actions)?raw.actions.slice(0,8):[]}}
function fieldValues(){const values={};for(const node of fieldsEl.querySelectorAll("[data-field-id]")){const id=node.dataset.fieldId;if(!id)continue;if(node instanceof HTMLInputElement&&node.type==="checkbox"){values[id]=node.checked;continue}if(node instanceof HTMLInputElement||node instanceof HTMLTextAreaElement){const value=node.value.slice(0,node instanceof HTMLTextAreaElement?2000:1000);if(node instanceof HTMLInputElement&&node.type==="number"&&value!==""){const parsed=Number(value);values[id]=Number.isFinite(parsed)?parsed:value}else values[id]=value}}return values}
function persist(){try{if(window.openai&&typeof window.openai.setWidgetState==="function")window.openai.setWidgetState({modelContent:"MSO block: "+(current?.title||"action"),privateContent:{values:fieldValues()},imageIds:[]})}catch(_){}}
function renderChecks(rows){clear(checksEl);checksSection.hidden=!rows.length;for(const row of rows){if(!row||typeof row!=="object")continue;const state=["pass","warn","fail","info"].includes(row.state)?row.state:"info";const item=el("div","check");item.append(el("span","dot "+state),el("strong","",text(row.label,"Check")),el("span","detail",text(row.detail,state)));checksEl.append(item)}}
function renderFields(rows){clear(fieldsEl);fieldsSection.hidden=!rows.length;rows.forEach((row,index)=>{if(!row||typeof row!=="object")return;const id=text(row.id,"field-"+index),controlId="mso-field-"+index,inputType=["text","number","email","url","textarea","boolean"].includes(row.input)?row.input:"text",editable=row.editable!==false;const wrap=el("div","field"+(inputType==="textarea"?" full":""));const label=el("label","",text(row.label,id));label.id=controlId+"-label";if(row.required===true)label.append(el("span","required"," *"));wrap.append(label);if(!editable){const value=el("div","readonly",String(scalar(row.value)));value.id=controlId;value.setAttribute("aria-labelledby",label.id);wrap.append(value);fieldsEl.append(wrap);return}let input;if(inputType==="textarea"){input=document.createElement("textarea");input.value=String(scalar(row.value)).slice(0,2000);input.maxLength=2000}else{input=document.createElement("input");input.type=inputType==="boolean"?"checkbox":inputType;if(inputType==="boolean")input.checked=Boolean(row.value);else input.value=String(scalar(row.value)).slice(0,1000);if(["text","email","url"].includes(inputType))input.maxLength=1000}input.id=controlId;label.htmlFor=controlId;input.name=id;input.dataset.fieldId=id;input.required=row.required===true;if(text(row.placeholder,""))input.placeholder=text(row.placeholder,"");input.addEventListener("input",persist);if(inputType==="boolean"){const booleanWrap=el("div","boolean");booleanWrap.append(input,el("span","",text(row.placeholder,"Enabled")));wrap.append(booleanWrap)}else wrap.append(input);fieldsEl.append(wrap)})}
function renderOutputs(rows){clear(outputsEl);outputsSection.hidden=!rows.length;for(const row of rows){if(!row||typeof row!=="object")continue;const item=el("div","output");item.append(el("span","dot info"),el("strong","",text(row.label,"Result")),el("span","value",String(scalar(row.value))));outputsEl.append(item)}}
async function submit(action){if(!action||typeof action!=="object")return;if(text(action.confirm,"")&&confirmId!==action.id){confirmId=text(action.id,"");feedback.textContent=text(action.confirm,"Press the action again to confirm.");window.clearTimeout(confirmTimer);confirmTimer=window.setTimeout(()=>{confirmId="";feedback.textContent=""},6000);return}confirmId="";window.clearTimeout(confirmTimer);if(!form.reportValidity())return;const payload={actionId:text(action.id,"action"),blockKind:current.kind,values:fieldValues()};const prompt=text(action.prompt,"Continue with the selected MSO action.")+"\n\nMSO block submission:\n"+JSON.stringify(payload,null,2).slice(0,12000);feedback.textContent="Sending to chat…";try{if(!window.openai||typeof window.openai.sendFollowUpMessage!=="function")throw new Error("unsupported");await window.openai.sendFollowUpMessage({prompt});feedback.textContent="Sent to chat."}catch(_){feedback.textContent="Interactive handoff is unavailable. Continue in the chat."}}
function renderActions(rows){clear(actionsEl);actionsSection.hidden=!rows.length;for(const row of rows){if(!row||typeof row!=="object")continue;const button=el("button",["primary","secondary","danger"].includes(row.style)?row.style:"secondary",text(row.label,"Continue"));button.type="button";button.addEventListener("click",()=>submit(row));actionsEl.append(button)}}
function render(raw){const next=normalize(raw);if(!next)return;current=next;kindEl.textContent=next.kind;titleEl.textContent=next.title;descriptionEl.textContent=next.description;descriptionEl.hidden=!next.description;statusEl.className="status "+next.status;statusEl.textContent=next.status==="neutral"?"Ready":next.status;renderChecks(next.checks);renderFields(next.fields);renderOutputs(next.outputs);renderActions(next.actions);persist()}
function readHostOutput(){const output=window.openai&&window.openai.toolOutput;if(output)render(output)}
window.addEventListener("message",event=>{if(event.source!==window.parent)return;const message=event.data;if(message&&message.method==="ui/notifications/tool-result"&&message.params&&message.params.structuredContent)render(message.params.structuredContent)},{passive:true});
window.addEventListener("openai:set_globals",readHostOutput,{passive:true});readHostOutput();
})();</script>
</main>`;

export const MSO_BLOCK_RESOURCE = {
  uri: MSO_BLOCK_URI,
  name: "MSO Block",
  description: "Compact validation, action, and CRUD input-output component for ChatGPT.",
  mimeType: MIME,
  text: html,
  _meta: widgetResourceMeta(
    "Compact MSO interaction block. It renders bounded text fields, validation states, outputs, and user-approved action handoffs; it never executes a mutation by itself.",
  ),
} as const;
