# MSO ZAP baseline reviewed exceptions.
# INFO keeps the signal visible without treating the documented design trade-off as a release failure.
# Any other ZAP baseline rule remains WARN by default and therefore fails the gate (exit 2).
10015	INFO	(Public web manifest intentionally revalidates; no sensitive content)
10049	INFO	(Private/authenticated responses intentionally use no-store; static auth-shell responses inherit it)
10055	INFO	(CSP permits arbitrary HTTPS image/embed sources by product design; connect-src remains tightly restricted)
10096	INFO	(Build/static bundle contains ordinary Unix timestamps; no credential or host-secret exposure)
90004	INFO	(COEP intentionally omitted because cross-origin media/embeds are supported; inbound framing is denied separately)
